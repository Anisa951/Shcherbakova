#Задача №1: Создание списка и получение элементов
# Создайте список с названием animals, содержащий в точности следующие строки: "кошка", "собака", "попугай", "хомяк".
# Выведите на экран (с помощью print) второй элемент этого списка.
# Выведите на экран последний элемент этого списка, используя отрицательный индекс.
from operator import index
from os import remove

animals =["кошка", "собака", "попугай", "хомяк"]
print(animals[1])
print(animals[-1])

#Задача №2: Удаление элементов из списка и его очистка
# У вас есть список инструментов:
# tools = ["отвертка", "пассатижи", "молоток", "рулетка", "уровень", "дрель"]
# Удалите из списка элемент "молоток" с помощью метода, который требует указать его точное значение.
# Удалите из списка второй элемент (по индексу) с помощью метода, который требует указать его индекс.
# (После первого удаления список изменился, пересчитайте индекс нужного элемента).
# Очистите весь список, оставив его пустым.
# На каждом шаге выводите список tools с помощью print, чтобы видеть изменения.

tools = ["отвертка", "пассатижи", "молоток", "рулетка", "уровень", "дрель"]
tools.remove("молоток")
print(tools)
del tools[1]
print(tools)
tools.clear()
print(tools)

#Задача №3: Срезы списка
# Дан список чисел: numbers = [5, 10, 15, 20, 25, 30, 35, 40, 45, 50]
# Используя срезы, создайте и выведите на экран (через print) следующие новые списки:
# Первые три элемента списка numbers.
# Элементы с четвертого по седьмой (включительно).
# Все элементы, начиная с пятого и до конца.
# Каждый второй элемент всего списка numbers (шаг 2).

numbers = [5, 10, 15, 20, 25, 30, 35, 40, 45, 50]
three_numbers = numbers[:3]
print(three_numbers)
four_numbers = numbers[4:8]
print(four_numbers)
five_numbers = numbers[5:]
print(five_numbers)
second_num = numbers[::2]
print(second_num)

#Задача №4: Добавление элементов в список и их замена
# У вас есть изначальный список:
# colors = ["красный", "синий", "зеленый"]
# Выполните следующие действия последовательно и выводите список после каждого шага:
# Добавьте в конец списка строку "желтый".
# Замените (перезапишите) второй элемент списка на строку "фиолетовый".
# Добавьте в конец списка еще два элемента сразу: "оранжевый" и "белый". Сделайте это одной операцией.

colors = ["красный", "синий", "зеленый"]
colors.append("желтый")
print(colors)
colors[1] = "фиолетовый"
print(colors)
colors2 = ["оранжевый","белый"]
colors.extend(colors2)
print(colors)

#Задача №5: Слияние списков
# У вас есть два списка:
# list_a = [1, 2, 3]
# list_b = [4, 5, 6]
# Ваша задача — создать новый список (не изменяя исходные list_a и list_b) тремя разными способами и вывести каждый результат:
# Способ 1: Используйте оператор + для слияния.
# Способ 2: Используйте метод extend(), но так, чтобы исходные списки не изменились (продумайте, как это сделать).
# Способ 3: Используйте срезы для добавления всех элементов второго списка к первому в новом списке.
# Выведите на экран три получившихся новых списка. Убедитесь, что исходные list_a и list_b остались неизменными.

list_a = [1, 2, 3]
list_b = [4, 5, 6]
print(list_a+list_b)
copy_list =list_a.copy()
copy_list.extend(list_b)
print(copy_list)
copy_list_a = list_a.copy()
print(copy_list_a[:]+list_b[:])

# Задача №6: Добавление элементов в конкретное место списка
# У вас есть список:
# fruits = ["яблоко", "банан", "киви"]
# Выполните следующие действия и выводите список после каждого шага:
# Вставьте строку "апельсин" в начало списка (на позицию с индексом 0).
# Вставьте строку "манго" между "банан" и "киви" (определите правильный индекс).
# Вставьте строку "груша" на позицию с индексом 2.
# Напишите ваш код.

fruits = ["яблоко", "банан", "киви"]
fruits.insert(0,"апельсин")
print(fruits)
fruits.insert(3,"манго")
print(fruits)
fruits.insert(2,"груша")
print(fruits)

# #Задача№ 7: Замена элементов в списке
# У вас есть список:
# books = ["Математика", "Физика", "Химия", "История", "География"]
# Вам нужно сделать следующие замены в исходном списке и вывести итоговый результат:
# Замените элемент с индексом 1 на строку "Биология".
# Замените элемент с индексом 3 на строку "Литература".
#Замените последний элемент списка на строку "Искусство". Используйте отрицательный индекс

books = ["Математика", "Физика", "Химия", "История", "География"]
books[1] = "Билогия"
books[3] = "Литература"
books[-1] = "Искусство"
print(books)

# Задача №8: Создание списка и получение элементов (вариант 2)
# Создайте список с названием mixed_list, который будет содержать ровно следующие элементы в указанном порядке:
# Целое число 100
# Число с плавающей точкой 3.14
# Строку "текст"
# Булево значение True
# Выведите на экран первый элемент этого списка.
# Выведите на экран третий элемент этого списка.
# Выведите на экран длину (количество элементов) этого списка с помощью встроенной функции.
# Напишите ваш код.

mixed_list =[100, 3.14, "текст", True]
print(mixed_list[0])
print(mixed_list[2])
print(len(mixed_list))

# Задача №9: Комбинированная работа со списком (создание, добавление, удаление, срезы)
# Создайте пустой список с именем task_list.
# Добавьте в него строки "Понедельник", "Вторник", "Среда" одной командой (методом, который принимает последовательность).
# Добавьте строку "Четверг" в конец списка.
# Добавьте строку "Воскресенье" в начало списка.
# Удалите из списка строку "Среда" (по значению).
# Используя срезы, создайте новый список weekend, содержащий последние два элемента из task_list.
# Выведите на экран:
# Итоговый task_list.
# Список weekend.
# Длину списка task_list.

task_list = []
task_list.extend(["Понедельник", "Вторник", "Среда"])
task_list.append("Четверг")
task_list.insert(0,"Воскресенье")
task_list.remove("Среда")
weekend = task_list[-2:]
print(task_list)
print(weekend)
print(len(task_list))

# Задача №10: Комбинированная работа (замена, слияние, срезы)
# Создайте список main = [10, 20, 30, 40, 50, 60].
# Создайте второй список extra = [70, 80].
# Замените в списке main элементы с индексами 2 и 4 на числа 300 и 500 соответственно (сделайте это двумя отдельными строками кода).
# Слейте список extra с концом списка main так, чтобы main изменился. Сделайте это одним методом.
# Используя срез, получите из итогового списка main новый список middle, содержащий элементы с 3-го по 5-й (включительно).
# Считайте элементы в обычном порядке, начиная с 1.
# Выведите на экран:
# Итоговый список main.
# Список middle.
# Последний элемент списка main, используя отрицательную индексацию.
# Напишите ваш код.

main = [10, 20, 30, 40, 50, 60]
xtra = [70, 80]
main[2] = 300
main[4] = 500
main.extend(xtra)
middle = main[2:5]
print(main)
print(middle)
print(main[-1])

# Задача №11: Работа с индексами и значениями (поиск, замена, вставка)
# Создайте список: items = ["стол", "стул", "лампа", "книга", "ручка", "стул", "чашка"].
# Найдите и выведите индекс первого вхождения строки "стул" в списке.
# Найдите и выведите индекс второго вхождения строки "стул" в списке. (Используйте результат предыдущего поиска).
# Замените элемент, который находится ровно посередине списка (рассчитайте индекс для любой длины), на строку "КАРТИНА".
# Вставьте строку "ТЕТРАДЬ" сразу после первого элемента списка.
# Выведите итоговый список items.
# Подсказка: для поиска второго вхождения используйте метод .index() с необязательным параметром start.
items = ["стол", "стул", "лампа", "книга", "ручка", "стул", "чашка"]
item_index1 = items.index("стул")
print(item_index1)
item_index2 = items.index("стул",2)
print(item_index2)
items[len(items) // 2] = "КАРТИНА"
items.insert(1,"ТЕТРАДЬ")
print(items)

# Задача №12: Базовые операции со списками (повторение и комбинация)
#
# Создайте список: cities = ["Москва", "Сочи", "Казань", "Новосибирск"].
# Добавьте в конец списка город "Екатеринбург".
# Вставьте город "Калининград" на позицию перед последним элементом списка (рассчитайте индекс).
# Удалите из списка город "Сочи" по значению.
# Замените второй элемент списка на "Санкт-Петербург".
# Используя срез, создайте новый список two_cities, содержащий только первые два элемента изменённого списка cities.
# Выведите на экран:
# Итоговый список cities.
# Список two_cities.
# Длину списка cities.

cities = ["Москва", "Сочи", "Казань", "Новосибирск"]
cities.append("Екатеринбург")
cities.insert(-1, "Калининград")
cities.remove("Сочи")
cities[1] = "Санкт-Петербург"
two_cities = cities[:2]
print(cities)
print(two_cities)
print(len(cities))